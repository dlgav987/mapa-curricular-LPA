function mostrarInfo(materia) {
  const infoBox = document.getElementById("info");
  if (materia === "mat1") {
    infoBox.innerHTML = "<strong>Matemáticas I:</strong> Introducción al álgebra. Prerrequisito: Ninguno.";
  }
}
